{
  "name": "mongooqs",
  "version": "1.0.0",
  "description": "Express.js middleware to create MongoDB queries with mongoose through querystring",
  "main": "index.js",
  "scripts": {
    "start": "yarn install || npm install",
    "test": "mocha test/**/*.js"
  },
  "author": "Johnny Gabriel <johnny_gabrie@outlook.com>",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^3.2.0"
  }
}
